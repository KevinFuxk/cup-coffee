# large, regenerable chart cache — not needed in the repo (cloud refetches only the new day)
cache/
# logs and OS cruft
*.log
.DS_Store
__pycache__/
*.pyc
# local-only backups / snapshots
events_2y.jsonl
mined_table_2y.json
factor_scores_2y.json
# never commit secrets
.env
*.key
