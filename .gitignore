# large, regenerable chart cache — not needed in the repo (cloud refetches only the new day)
cache/
# generated trade charts (regenerable via viz_batch.py)
charts/
# logs and OS cruft
*.log
.DS_Store
__pycache__/
*.pyc
# regenerable data state (rebuilt from code + APIs; not source) and local backups
events*.jsonl
mined_table*.json
realprice*.json
commodity*.json
entry_type*.json
cup_strict*.json
factor_scores*.json
factor_track*.jsonl
*.checkpoint
*.csv
# never commit secrets
.env
*.key
# raw market-data slices + generated data artifacts (data stays local by design)
data/splits/
data/*.html
data/watchlist.txt
data/idx_multitf.json
data/*.superseded
# editor + local backups + shell accidents
.vscode/
